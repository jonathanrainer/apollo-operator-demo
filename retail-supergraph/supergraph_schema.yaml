apiVersion: apollographql.com/v1alpha1
kind: SupergraphSchema
metadata:
  name: retail
spec:
  selectors:
    - matchLabels:
        domain: retail
  graphRef: operator-test@summit-demo
  suspend: false
